---
const {
  title,
  subtitle,
  primary,
  secondary,
  kicker,
  backgroundClass = "",
} = Astro.props;
---

<section class={`page-hero ${backgroundClass}`}>
  <div class="container-wide">
    <div class="page-hero-inner">
      {kicker
        ? <p class="section-label">{kicker}</p>
        : <p class="section-label" aria-hidden="true">Abstract Machines</p>
      }
      <h1 class="page-hero-title">{title}</h1>
      {subtitle && <p class="page-hero-subtitle">{subtitle}</p>}
      {(primary || secondary) && (
        <div class="page-hero-ctas">
          {primary && (
            <a
              class="button-primary"
              href={primary.href}
              target={primary.external ? "_blank" : undefined}
              rel={primary.external ? "noopener noreferrer" : undefined}
            >
              {primary.label}
            </a>
          )}
          {secondary && (
            <a
              class="button-secondary"
              href={secondary.href}
              target={secondary.external ? "_blank" : undefined}
              rel={secondary.external ? "noopener noreferrer" : undefined}
            >
              {secondary.label}
            </a>
          )}
        </div>
      )}
    </div>
    <slot />
  </div>
</section>

<style>
  .page-hero {
    padding-top: clamp(3rem, 6vw, 5rem);
    padding-bottom: clamp(2.5rem, 4vw, 4rem);
    text-align: center;
  }

  .page-hero-inner {
    max-width: 52rem;
    margin-inline: auto;
  }

  .page-hero-title {
    font-size: clamp(2.25rem, 5vw, 3.75rem);
    font-weight: 700;
    letter-spacing: -0.035em;
    line-height: 1.08;
    color: var(--text);
    margin-top: 0.65rem;
  }

  .page-hero-subtitle {
    font-size: clamp(0.95rem, 1.35vw, 1.05rem);
    color: var(--muted);
    line-height: 1.75;
    margin-top: 0.9rem;
    max-width: 58ch;
    margin-inline: auto;
  }

  .page-hero-ctas {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 1.75rem;
    justify-content: center;
  }
</style>
